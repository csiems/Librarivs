<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="buttonStyle" parent="android:TextAppearance">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="fontPath">fonts/sunday.ttf</item>
    </style>

    <style name="TextAppearance.FontPath" parent="android:TextAppearance">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="fontPath">fonts/Montserrat-Bold.ttf</item>
    </style>

    <style name="formTextViewStyle" parent="android:TextAppearance">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="fontPath">fonts/Montserrat-Regular.ttf</item>
    </style>


</resources>
